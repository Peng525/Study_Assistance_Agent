# 项目技术准备方案 v3.0

> 基于：项目提议书 V1.2 + 灵感来源.txt + 热门 Skill 文档 + 多轮需求澄清
> 日期：2026-08-23
> 状态：已确认，开始执行

---

## 一、项目范围（经多轮澄清确认）

### 1.1 项目定位

这不是纯演示 demo，也不是完整 B 站级平台，而是**分阶段演进的 AI 助学产品**：

| 阶段 | 目标 | 范围 | 交付对象 |
|---|---|---|---|
| **Phase 0（当前）** | 跑通"AI 助学"核心功能 | 视频→暂停→选中字幕→右键→AI 答疑链路 + 管理台配置大模型 + 可换素材 | B 站评审 + 自用 |
| **Phase 1+（后续）** | 完整学习平台 | 视频上架、学习栏目、历史记录、上传视频、用户系统 | 自用产品 |

**关键澄清**：
- 用户明确"第一阶段只想跑通 agent 助学功能"，视频上架/栏目/历史/上传等都是后续阶段
- 项目既是给 B 站评审用，也是用户自己的潜在独立产品（两者都是）
- 大模型 API 由"管理台后端"配置，管理台具体范围由架构师评估
- 课程素材由用户自备

### 1.2 Phase 0 范围细化

**包含**：
- 核心交互链路：播放视频→暂停→选中字幕→右键唤起→AI 结合课件+逐字稿答疑→多轮追问
- 管理台：配置大模型 API（key/baseUrl/模型名等），架构师评估是否还管别的
- 可换素材：用户能用自备的课程素材（视频+VTT字幕+课件），不是硬编码固定素材
- 完整播放器交互体验（参考 B 站/学习平台的播放器形态，但不是复刻 B 站全站）

**不包含（明确推到 Phase 1+）**：
- 视频上架流程、学习栏目分类、观看历史记录、视频上传功能
- 用户系统（登录注册、个人主页）
- UGC 平台属性（别人上架、用户看别人上架的）

### 1.3 与提议书 V1.2 的差异

| 维度 | 提议书 V1.2 | 本方案 Phase 0 |
|---|---|---|
| 定位 | 纯演示原型，不碰真实场景 | 可直接拿来的最小可用版，既评审又自用 |
| 素材 | 预置固定示例 | 用户可换自备素材 |
| 大模型 API | 硬编码公有 API | 管理台配置 |
| 后端 | 无后端 | 需要轻量后端（管理台配置 + 架构师评估的存储） |
| 复杂度 | 中等偏轻 | 中等（比纯前端原型重，比完整平台轻） |

**Phase 0 不是纯前端项目**，需要轻量后端支持管理台配置。具体后端方案由架构师 subagent 评估。

---

## 二、Subagent 体系（6 角色 + 主 Agent）

### 总览

```
主 Agent（总调度 + 唯一业务编码者）
  │  调用 skill：ui-ux-pro-max + vercel-react-best-practices + vercel-composition-patterns
  │
  ├── 1. 需求评审 Subagent     ← grill-me（兼顾轻量商业视角）
  ├── 2. PRD Subagent          ← prd-generator
  ├── 3. 架构师 Subagent       ← Phase0:前端+轻量后端架构 / Phase1+:api-design-principles
  ├── 4. 代码检查 Subagent     ← code-reviewer
  ├── 5. 测试 Subagent         ← webapp-testing
  └── 6. 版本管理 Subagent     ← pr-creator + writing-changelogs（合并）
```

### 各角色详细设计

#### 1. 需求评审 Subagent（grill-me，兼顾轻量商业视角）

| 项 | 内容 |
|---|---|
| 嵌入 Skill | grill-me |
| 工具权限 | Read、Glob、Grep（只读） |
| Phase 0 职责 | 拷问提议书 V1.2 + 灵感来源：技术边界遗漏 + 轻量方案合理性（质疑脱离实际的假设，但不设独立商业维度） |
| Phase 1+ 职责 | 拷问完整产品需求：用户系统、多页面、存储方案的技术边界 + 方案合理性 |
| 产出 | 审查报告返回主会话（不写文件，篇幅小） |
| 调用时机 | 每个 Phase 开发前 |

#### 2. PRD Subagent（prd-generator）

| 项 | 内容 |
|---|---|
| 嵌入 Skill | prd-generator |
| 工具权限 | Read、Glob、Grep、Write（只读源码，可写 `_scratch/`） |
| Phase 0 职责 | 基于提议书 + 灵感来源 + 评审报告，深化为开发级 PRD：用户故事、验收标准、交互流程图、MVP 边界（明确 Phase 0 范围） |
| Phase 1+ 职责 | 完整产品 PRD：登录注册、首页、学习页、个人主页、AI 答疑的功能规格 |
| 产出 | `_scratch/prd.md` |
| 调用时机 | 需求评审通过后 |

#### 3. 架构师 Subagent（分阶段激活不同内容）

| 项 | 内容 |
|---|---|
| Phase 0 嵌入内容 | 前端架构 + **轻量后端架构**：前端框架选型、ArtPlayer 集成、**字幕选中交互技术路线（核心风险）**、**管理台后端技术选型**、**素材存储方案（本地 vs 对象存储）**、**大模型 API 配置存储方案**、组件分层、状态管理 |
| Phase 1+ 嵌入 Skill | api-design-principles（完整 API 设计、鉴权、数据库设计、对象存储方案） |
| 工具权限 | Read、Glob、Grep、Write（只读源码，可写 `_scratch/` 架构文档） |
| 产出 | `_scratch/architecture.md`（含技术栈推荐 + 架构图 + 技术风险预案） |
| 调用时机 | PRD 确认后、开发前。**第一个产出是技术栈推荐，用户确认后再进开发** |

#### 4. 代码检查 Subagent（code-reviewer）

| 项 | 内容 |
|---|---|
| 嵌入 Skill | code-reviewer |
| 工具权限 | Read、Glob、Grep、Write（只读 src/，可写 `_scratch/` 报告 + `.codebuddy/code-review-pass.flag` 标记） |
| 职责 | 本地变更代码审查：逻辑 Bug、并发安全、性能隐患、可落地修改建议；审查通过后自行写入 PASS 标记 |
| 产出 | `_scratch/code-review.md` + `.codebuddy/code-review-pass.flag` |
| 调用时机 | 主 Agent 完成功能模块后 |

#### 5. 测试 Subagent（webapp-testing）

| 项 | 内容 |
|---|---|
| 嵌入 Skill | webapp-testing（Playwright） |
| 工具权限 | Read、Glob、Grep、Bash、Write（只读 src/，可写 `*.test.*` + `_scratch/` + `.codebuddy/test-pass.flag` 标记） |
| 职责 | E2E 自动化测试，验证核心链路可复现；测试通过后自行写入 PASS 标记 |
| 产出 | `_scratch/test-report.md` + `tests/*.test.*` + `.codebuddy/test-pass.flag` |
| 调用时机 | 代码检查通过后 |

#### 6. 版本管理 Subagent（pr-creator + writing-changelogs 合并）

| 项 | 内容 |
|---|---|
| 嵌入 Skill | pr-creator + writing-changelogs |
| 工具权限 | Read、Glob、Grep、Bash（git 操作）、Write（CHANGELOG.md） |
| 职责 | 规范 git 提交（Conventional Commits）+ 生成变更日志/Release Notes |
| 产出 | git commit + CHANGELOG.md 更新 |
| 调用时机 | 测试通过后、阶段性交付时 |

### 主 Agent 职责

- 总调度：按工作流依次派生各 Subagent，读取产出，决策推进
- 唯一业务编码者：所有 `src/` 源码由主 Agent 写
- 项目搭建、依赖安装、素材准备
- 调用 3 个开发 Skill：ui-ux-pro-max（UI 组件）+ vercel-react-best-practices + vercel-composition-patterns

---

## 三、权限管控矩阵

| 角色 | 可读 | 可写 | 可执行 | 禁止 |
|---|---|---|---|---|
| 主 Agent | 全部 | 全部（含 src/） | 全部 | 无 |
| 需求评审 | 文档 | 无 | 无 | 写任何文件 |
| PRD | 源码、文档 | 仅 `_scratch/*.md` | 无 | 写 src/、bash |
| 架构师 | 源码、文档 | 仅 `_scratch/*.md` | 无 | 写 src/、bash |
| 代码检查 | 源码 | `_scratch/*.md` + `.codebuddy/code-review-pass.flag` | 无 | 写 src/、bash |
| 测试 | 源码 | `*.test.*` + `_scratch/*.md` + `.codebuddy/test-pass.flag` | 运行测试 | 写 src/ 业务代码 |
| 版本管理 | 全部 | CHANGELOG.md + 清空 `.codebuddy/*-pass.flag` 为 PENDING | git 命令 | 写 src/ 业务代码 |

**铁律**：业务源码 `src/` 修改权永远只在主 Agent。Subagent 发现问题写报告，主 Agent 改。

---

## 四、Skill 分配总表

| Skill | 归属 | 阶段 |
|---|---|---|
| grill-me | 需求评审 Subagent | 全阶段 |
| prd-generator | PRD Subagent | 全阶段 |
| api-design-principles | 架构师 Subagent | Phase 1+ 才激活（Phase 0 用前端+轻量后端架构内容） |
| ui-ux-pro-max | 主 Agent | 全阶段 |
| vercel-react-best-practices | 主 Agent | 全阶段 |
| vercel-composition-patterns | 主 Agent | Phase 1+（原型组件量不大，可选用） |
| code-reviewer | 代码检查 Subagent | 全阶段 |
| webapp-testing | 测试 Subagent | 全阶段 |
| pr-creator | 版本管理 Subagent | 全阶段 |
| writing-changelogs | 版本管理 Subagent | 全阶段 |

**不选的 Skill**：web-design-guidelines、frontend-design、canvas-design、python-design-patterns、code-review-excellence、audit-website、grill-with-docs、agent-browser（主 Agent 直接用浏览器自动化）。

---

## 五、Phase 0 工作流

```
0a 需求评审    主Agent派生「需求评审」→ 拷问提议书V1.2+灵感来源（技术边界+轻量商业）→ 报告返回
    ↓
0b PRD深化     主Agent派生「PRD」→ 基于提议书+灵感来源+评审报告 → _scratch/prd.md
    ↓              （明确Phase 0范围：核心助学链路 + 管理台配置 + 可换素材）
    ↓
0c 架构评估    主Agent派生「架构师」→ 评估前端+轻量后端技术栈 + 字幕选中方案 + 管理台方案
    ↓           → _scratch/architecture.md
    ↓           用户确认技术栈后
0d 项目搭建    主Agent初始化项目（前端+后端）+ ArtPlayer集成 + 素材目录
    ↓
0e 功能开发    主Agent编码（加载ui-ux-pro-max+vercel-react-best-practices）
              前端：ArtPlayer+VTT字幕 → 字幕选中+右键菜单 → AI面板+流式输出 → 多轮追问
              后端：管理台配置接口 + 素材读取接口
    ↓
0f 代码检查    主Agent派生「代码检查」→ _scratch/code-review.md → 主Agent修复
    ↓
0g 测试验证    主Agent派生「测试」→ E2E验证核心链路 → _scratch/test-report.md
    ↓
0h 版本管理    主Agent派生「版本管理」→ git提交+CHANGELOG
    ↓
0i 交付        主Agent录屏+整理交付物 → 可提交B站评审 + 自用
```

---

## 六、产物目录规范

```
项目根目录/
├── src/                    ← 业务源码（仅主 Agent 写）
├── public/
│   ├── videos/             ← 示例视频
│   ├── subtitles/          ← VTT 字幕
│   └── courseware/         ← Markdown 课件
├── tests/                  ← 测试文件（测试 Subagent 可写）
├── _scratch/               ← Subagent 产出（gitignore 忽略）
│   ├── prd.md
│   ├── architecture.md
│   ├── code-review.md
│   └── test-report.md
├── _archive/               ← 归档的旧文档
├── CHANGELOG.md            ← 版本管理 Subagent 维护
├── .gitignore              ← 含 _scratch/、_archive/
├── 项目提议书.docx          ← 已有
├── 灵感来源.txt             ← 已有
└── 项目技术准备方案.md      ← 本文档
```

---

## 七、当前执行进度

- [x] v3 技术准备方案确认
- [x] 归档 CEO-subagent-角色设计文档.md 到 `_archive/`
- [x] 更新项目技术准备方案.md 为 v3
- [ ] **Phase 0a：需求评审（进行中）**
- [ ] Phase 0b：PRD 深化
- [ ] Phase 0c：架构评估
- [ ] Phase 0d~0i：搭建→开发→检查→测试→版本→交付
